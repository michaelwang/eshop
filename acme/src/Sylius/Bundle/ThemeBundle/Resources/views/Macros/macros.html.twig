{% macro breadcum(menu) %}
                <div class="col-lg-12 col-md-12 col-sm-12">
                        <div class="breadcrumbs">
                        <p><a href="/">Home</a> <i class="icons icon-right-dir"></i> {{ menu }} <i class="icons icon-right-dir"></i> </p>
                    </div>
                </div>
{% endmacro %}

{% macro pagination(paginator) %}
  {% if paginator.haveToPaginate() %}
       {{ pagerfanta(paginator, 'homeshop_view') }}
  {% endif %}
{% endmacro %}
